# Supabase / PostgreSQL
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=tu_password
DB_HOST=db.tu-proyecto.supabase.co
DB_PORT=5432

# Django
SECRET_KEY=tu-secret-key-aqui
DEBUG=True

# Usar SQLite en local si Supabase no conecta (runserver se queda colgado)
# USE_SQLITE_DEV=True

# Email (opcional, para notificaciones)
EMAIL_HOST=smtp.ejemplo.com
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_PORT=587
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=noreply@fenix.com
