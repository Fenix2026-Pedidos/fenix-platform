# 🌐 Internacionalización e Idiomas (i18n)

Plataforma Fenix ofrece soporte completo multi-idioma, centrado inicialmente en Español (ES) y Chino Simplificado (ZH-Hans).

## Sistemas de Traducción
El proyecto utiliza dos sistemas complementarios:

### 1. Interfaz de Usuario (Django i18n)
Traduce menús, botones y mensajes estáticos del sistema.
- **Selector**: Ubicado en el topbar (header).
- **Almacenamiento**: Persiste vía cookies (`django_language`) y sesión.
- **Archivos**: Localizados en `locale/`.

### 2. Contenido de Productos (Traducción Automática)
Permite traducir nombres y descripciones de productos desde el Admin de Django.
- **Motor**: Utiliza la librería `deep-translator` (Google Translate).
- **Funcionamiento**: 
    - Botón manual en la edición de productos.
    - Traducción automática al guardar si faltan los campos en chino.

## Guía de Uso para Managers
1. **Creación**: Al añadir un producto en español, el sistema intentará traducirlo automáticamente al chino al guardar.
2. **Corrección**: Si la traducción automática no es precisa, se puede ajustar manualmente en los campos `name_zh_hans` y `description_zh_hans`.

## Mantenimiento Técnico
- **Agregar nuevos textos**: Usar `{% trans %}` en templates y ejecutar `makemessages`.
- **Compilar**: Es necesario ejecutar `compilemessages` para que los cambios en los archivos `.po` se reflejen en el sitio.
